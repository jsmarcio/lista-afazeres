export class Item {

    nome: string;
    data: string;
    descricao: string;

    constructor(nome: string, data: string, descricao: string) {
        this.nome= nome;
        this.data = data;
        this.descricao = descricao;
    }
}